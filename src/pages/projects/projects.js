import React from 'react';
import ProjectsTable from '../../components/projects-table';

const Projects = () => (
  <div>
    <h2>View Projects</h2>
    <ProjectsTable />
  </div>
);

export default Projects;
