import React from 'react';
import TicketsTable from '../../components/tickets-table';

const Tickets = () => (
  <div>
    <h2>View Tickets</h2>
    <TicketsTable />
  </div>
);

export default Tickets;
